module top(
    input clk,
    input t,
    output reg q
);
    initial q = 0; // Initialize to known state
    // Write your code here
    always @(posedge clk) begin
        if (t)
            q <= ~q;   
    end
endmodule
