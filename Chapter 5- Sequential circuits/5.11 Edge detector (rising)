module top(
    input clk,
    input in,
    output out
);

    reg in_prev;
    reg out_reg;

    assign out = out_reg;

    always @(posedge clk) begin
        out_reg <= in & ~in_prev;
        in_prev <= in;
    end

endmodule
